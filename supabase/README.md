# Supabase — CYA Hub v10

Project ref: `rxgrhmnhmopxsiqndgmy`
Region: `eu-west-3`
Environment: `staging`

## Bootstrap rule

This directory intentionally contains **zero migrations** and **zero business schema** during bootstrap 00.1.

Legacy migrations imported: **0**.
Legacy schema imported: **0**.

The first future migration may only be created after an approved v10 functional contract and must be traceable:

CONTRATO → V10-FUNC → MOTOR OWNER → DECISIÓN → QA

Never copy migrations, dumps, tables, functions, policies, triggers, SECURITY DEFINER functions, or Edge Functions from CYA Hub v3.
